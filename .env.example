# ai-tools-core 环境配置
# 只包含环境相关配置，具体配置通过YAML文件管理

# 环境设置
APP__ENV=dev
APP__DEBUG=true
APP__PORT=9090

# 日志
LOGGER__LEVEL=debug

# 启用的平台 (all 或 xhs,dy,ks,bili,wb,tieba,zhihu)
# 示例: PLATFORM__ENABLED_PLATFORMS=xhs,dy  # 只启用小红书和抖音
# 示例: PLATFORM__ENABLED_PLATFORMS=all     # 启用所有平台
PLATFORM__ENABLED_PLATFORMS=bili,xhs

# 数据库配置 (PostgreSQL)
DB__HOST=localhost
DB__PORT=5432
DB__USER=postgres
DB__PASSWORD=ol59i29
DB__NAME=media_crawler_mcp

# Redis配置
REDIS__HOST=localhost
REDIS__PORT=6379
REDIS__PASSWORD=90ndfd980
REDIS__DB=0

# 是否无头模式: 是
BROWSER__HEADLESS=true
